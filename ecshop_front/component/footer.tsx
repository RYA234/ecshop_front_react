export default function Footer({ left, top, color, bgColor, height, width }: any) {
	return (
		<div>
		<footer>
					
				フッター
			
			<style jsx>{`
				footer{
					// display: flex;
					// position:relative;
					// left:${left}px;
					//top:${top}px;
					// width:100%;
					// width:${width}px;
					// height:${height}px;
					// color:${color};
					// background:${bgColor};	
					// z-index:0;
					// position: absolute;/*←絶対位置*/
					// bottom: 0; /*下に固定*/
				}
			`}</style>
		</footer>
		</div>
	)
}
